# Título completo do texto

Escreva aqui o corpo do artigo em Markdown normal (títulos com `##`, `###`,
listas, citações, imagens). Não repita título/autor/data no topo do arquivo —
isso já vem de `meta.yaml` e é renderizado automaticamente pelo `build.py`.
